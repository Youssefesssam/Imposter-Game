import 'package:flutter/material.dart';

class SelectCharacters extends StatelessWidget {
  const SelectCharacters({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
